import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// basic React 18 setup, might add error boundary and performance monitoring later
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
